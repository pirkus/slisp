; Sample program to exercise inline comment parsing
(defn -main []
  (let [a 1
        ; describe binding for b
        b 2
        result (+ a b)]
    ; trailing comment after expression
    (if (= result 3) 0 1))) ; end-of-line comment
