(defn -main []
  (let [prefix (subs "hello" 0 3)
        tail (subs "hello" 1 3)
        diff-prefix (- (count prefix) 3)
        diff-tail (- (count tail) 2)]
    (if (or diff-prefix diff-tail) 1 0)))
