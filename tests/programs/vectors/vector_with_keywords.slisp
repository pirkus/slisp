;; Test vectors containing keywords
(defn -main []
  (let [v1 (vec :a :b :c)
        v2 (vec :a :b :c)
        v3 (vec :a :b :d)
        result (if (= v1 v2)
                   (if (= v1 v3) 99 42)
                   99)]
    (if (= result 42) 0 1)))

;; Expected: 42
